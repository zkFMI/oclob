{
  "manifest_id": "oclob-native-browser-final-004",
  "contract_id": "oclob-native-http-v1",
  "contract_path": "research/oclob_native_http_contract.json",
  "contract_sha256": "720c4d2ee16d9412ca30840a3457c88a278d97515909a3521ce91c0b54306a15",
  "stage": "RUN_ROUGH_END_TO_END_AND_OBSERVE_FINAL_METRIC",
  "current_bottleneck": "Rough003 displayed actual settled levels in both viewports and removed them during a real upstream stop, but graph edges crossed neighbouring cards, global button padding hid zoom icons, and an internal-only Docker network did not publish the requested loopback port.",
  "causal_hypothesis": "Scoped button styles, non-overlapping evidence paths and an opt-in public-reader-only bridge will make the verified native view usable through its documented loopback endpoint while leaving private services isolated.",
  "single_intended_change": "Correct observed native UI integration defects and align display capacity with the existing nine-level circuit bound; repeat the unchanged complete financial/HTTP scenario and browser flow.",
  "baseline": "Rough003 at /tmp/oclob-native.eWmizS: 4 verified HTTP snapshots, 6 failure checks, 2 atomic fills/7500, 14 finality observations, post-canonical exit75 recovery, validator restart; browser showed15@100 and30@101 via diagnostic SSH-to-container tunnel. UI audit remained red.",
  "evaluation_population": { "hosts": 1, "corporate_clients": 2, "mpc_nodes": 7, "validators": 5, "orders": 4, "browser_viewports": ["1440x1000", "390x844"] },
  "primary_metric": "http_verified_depth_snapshots",
  "point_prediction": { "http_verified_depth_snapshots": 4, "http_fail_closed_checks": 6, "trade_notional": 7500, "autonomously_settled_fills": 2, "browser_viewports_showing_actual_depth": 2 },
  "most_likely_prediction_error": "Graph fitting or new port mapping may still be wrong at runtime despite passing type checks; snapshot expiry may be reached during browser inspection.",
  "rejection_rules": ["stale contract hash", "any original HTTP/native gate failure", "documented loopback port unavailable", "browser prices differ from actual TLS book", "mocked or legacy financial API", "stale rows shown as current during real outage", "clipped content, unreadable graph or invisible controls"],
  "promotion_rules": ["Native runner outcome remains smoke_only. UI acceptance additionally requires the final built assets, real desktop/narrow interactions and screenshots, type checks including Vite and test configs, component/unit tests and a separate browser receipt. No production or independent-operator claim."],
  "command": "make remote-native-http-e2e REMOTE_TEST_HOST=softbank-l40s REMOTE_TEST_SSH_OPTIONS='-o BatchMode=yes -o ProxyJump=none' NATIVE_HTTP_MANIFEST=/research/manifests/oclob_native_http_004.json NATIVE_BROWSER=1"
}
